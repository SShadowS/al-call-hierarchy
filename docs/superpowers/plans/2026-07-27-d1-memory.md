# d1 memory (plan)

Branch `feat/d1-memory`, base `70a4f8b` (master).

Scoped read-only first: `.superpowers/sdd/scope-d1-memory.md` (byte census + two probe
shapes) — **local scratch, gitignored (`.superpowers/sdd/.gitignore` is `*`), so it
exists in no clone.** Every load-bearing figure it produced is therefore restated in
full below and at its use site in the source; nothing tracked cites it for a number.
**Every number below is measured**; the census names its method per figure.

## What the census established (and corrected)

- **Process peak is 9,675 MB**, not the 11,825 quoted at kickoff — that predated the
  `feat/l3-substrate-and-parked-items` merge. Confirmed independently by the controller's
  own default-preset run at `70a4f8b`.
- **d1 contributes 3,743 MB of peak**, not ~4.3 GB. The 4.3 was a parent+children sum
  (`detector.d1-db-op-in-loop` is the parent span of `search_loops_cohorts` and
  `assemble_cohort_findings`). The honest measure is the peak d1 adds over the pre-d1 peak.
- **`rss_delta_mb` is commit-driven, not a live-bytes measure** — two runs of identical
  work disagreed by 500 MB on the same span, because the delta depends on how much freed
  heap the allocator reuses. **Use trace deltas for placement, a census for size.**
- d1 **does** set the process peak: it is reached inside `assemble_cohort_findings`, and
  all 42 other detectors together add 302 MB afterwards. d1 is 67.6 s of a 309 s run.

## The ceiling, stated before we start

d1's irreducible floor is **~190–250 MB live**; with allocator overhead a realistic
post-fix d1 is **~400–600 MB of peak**. **Sub-GB for the whole process is NOT reachable
here** — the process already stands at 5,932 MB *before* d1 runs. Landing d1 at ~500 MB
puts the process at ~6.2–6.5 GB with the L3 workspace and the cone substrate back on top.
This arc is measured against **d1's 3,743 MB**, not against sub-GB.

## Global Constraints (bind every task)

1. **Byte-identical output.** `scripts/check-goldens` (29 golden dirs across 9 targets)
   passes with **zero golden files touched**, and a DO-workspace `alsem analyze` run stays
   byte-identical. **Never blind-regen a golden.**
2. **No caps, sampling, or lossy truncation** — rejected by the user in the original d1
   arc. Every change here is representation and ownership only: same uncertainties, same
   witnesses, same findings.
3. `cargo check --all-targets` and `cargo clippy --all-targets --all-features` **zero
   warnings**. **`touch` changed files first** — a cached check prints "Finished" and
   proves nothing. Note `target/debug` was cleaned; the first check pays a full rebuild.
4. **A test must pin the USE, not just the helper, and must be proven able to fail** —
   see CLAUDE.md's Testing Philosophy. Five instances of that defect in the previous arc,
   all caught by review and none by the suite.
5. `rustfmt <file>` per file, never `cargo fmt`. Stage only intended paths, never
   `git add -A`.
6. **State the probe shape with every number.** `--detector d1-db-op-in-loop` and a
   default preset are different measurements and are never comparable.
7. Measure size with a census, not with `rss_delta_mb` (constraint set by finding 3 above).

---

## Task 1 — intern `CohortRep.uncertainties` (the largest single lever)

**~1.65–2.15 GB live, ~16–21 M allocations, zero output risk.** Replace the per-cohort
owned uncertainty payload with `Vec<u32>` ids into a run-level uncertainty table.
`uncertainty_lites` reads `kind`/`at` through the table; order is preserved.

Gate: goldens + DO byte-identical; census before/after for the *size* claim; trace for
placement only.

## Task 2 — shrink `Evidence`

**~940 MB of 1,055, ~14.8 M allocations, zero output risk.**
`Evidence { source, note }` → `{ source: &'static str, at: Arc<str>, kind: u8 }`, with the
`format!` deferred to `project_evidence`. `StableEvidence` must materialise the identical
`String` — that is the byte-identity argument and it needs a test that states it.

**As built (DONE):** `{ source: &'static str, note: Option<Arc<str>> }` — 32 B rather
than the planned shape's 40, with the `format!` moved UP to the interning boundary
(once per distinct uncertainty, 3,150 executions) rather than down to
`project_evidence` (7.4M). The planned `kind: u8` was rejected: `Uncertainty.kind` is
an open `String` from the L4 summary layer, so a discriminant would need a panic or a
`String` fallback for an eighth kind — a correctness hazard inside a change that is
meant to be representation only. Interning also makes the byte-identity argument
structural (the same `format!` expression with the same arguments, at a different
frequency) instead of a re-derivation that has to be proven equal. Measured: the
`confidence` bucket 1,055.5 MB / 14,924,347 allocations → 228.2 MB / 89,722; process
peak 7,531 → 6,434 MB. See `.superpowers/sdd/task-2-report.md`.

## Tasks 3-5 SUPERSEDED — re-scoped 2026-07-27 after Task 2 (see `.superpowers/sdd/task-2-review.md`)

**Why.** Two traces show the process peak is reached at `detector.d19-unused-parameter`
in BOTH base and HEAD — *after* `detect_d1` drops its transients. On HEAD d1's own
high-water is **176 MB below** the process peak. Therefore **a saving that removes only a
transient inside `detect_d1` moves the process peak by zero; only RETAINED reductions
convert 1:1.** The original Tasks 3-5 were sized in peak terms against the pre-Task-1
baseline, when d1 still set the peak.

**Also note the metric hazard this exposes:** "d1's contribution to peak" is measured on a
d1-only probe, where d1 is by construction the peak-setter. That metric rewards
transient-only work that the default-preset peak would not notice. The capstone must
report retained census bytes as the primary figure.

## Task 3 (consolidated) — the retained remainder, in value order

Target **≈ −293 MiB retained**, landing d1 at ≈230 MiB, and the process peak
6,434 → ~6,140 MB.

> **As built (DONE).** Retained `DetectorOutput` measured **195.0 MiB** in **957,972**
> live allocations; default-preset process peak **6,411 MB** (`peak_mb`), wall 197 s.
> **The "inside the 190-250 MiB floor band" framing this section originally carried is
> WITHDRAWN.** That band prices ~76-141 MiB of d1's *transients* (graph/liveness/SCC/
> terminal plan, the solver arena, the reaching-loop bitmaps) which are not in
> `DetectorOutput` at all; its retained-only portion is **~110 MiB**, so 195.0 MiB is
> ~1.8× it — and the planned ~230 MiB would not have been inside it either. The named
> closer is `FindingConfidence`-as-ids (**−78 MiB**), recorded in `docs/OUTSTANDING.md`
> and deliberately not built here. **d1 has not reached its floor; do not record that it
> has.**

| item | MiB retained | readers to prove |
|---|---:|---|
| 16-B `Evidence`: drop `source` from confidence evidence (`distinct source = 1` measured; every producer is a compile-time literal) | −113.2 | 3 |
| stop building `evidence_path`/`additional_paths` for cohort-bearing findings — **plus `gate/run.rs:699-702`, which the old Task 4 list missed** | −95.9 | 6 |
| intern `EvidenceStep`'s routine/callsite/op/anchor ids | −50 | 0 (internal) |
| intern `affected_objects`; `&'static str` for `title`/`fix_options` (563,126 object-id `String`s over 2,042 distinct values; `title` is 22,383 allocations of ONE distinct string) | −30 | 0 |
| `LoopSetRegistry` double-stored words | −4 | 0 |

**DROPPED — sharing one witness between the sink and `cohort_contexts` (~95 MB).** It is
**peak-only**: the retained side keeps exactly one witness payload either way, and the copy
that disappears is already freed before the process peak. Worth ~0 MB to a user. Dropping
it and saying why is more honest than banking it.

Also fold in the Task-2 review's Important 1: `UncertaintyLite`'s fields are `pub`, so the
"one place the note is produced" invariant is documented but no longer structural — it was
structural before Task 2 (`confidence.rs:41-47`).

## Task 4 — re-measure and capstone

Both probe shapes. **Report retained census bytes as the primary figure**, peak second.
Record honestly: retained 1,351.2 → 523.3 MiB so far, allocations 18,312,480 → 3,455,472,
process peak 9,675 → 6,434 MB. **Do not write "d1 reached its floor"** — allocations are at
the floor, bytes are ~2× it.

## Deliberately NOT in this arc

**Interning `ctx.uncertainties_by_node` at the substrate (census item #10).** Parked here
because it touches **51 detector files** via `to_confidence` and is substrate work, not d1
work — putting the arc's biggest blast radius before its biggest win.

> **Re-priced 2026-07-27 (Task-2 review).** It is **729.1 MiB in 7,428,267 allocations**,
> alive for the whole run — **larger than everything d1 retains put together (523.3 MiB)**,
> and drawing from the *same* 3,073-value vocabulary Task 2 just interned one rung
> downstream. Task 2 has now **built the substrate it needs** (`UncertaintyTable`,
> `UncertaintyLite`, `uncertainty_at`, and a `to_confidence` whose signature is the only
> thing those 51 files touch), so the blast-radius argument that parked it is materially
> weaker than when this plan was written. **It should be the next arc**, and the capstone
> says so.
>
> **Superseded 2026-07-27 (uncertainty-substrate arc, `442df6f`/`13ebcf7`).** Both figures
> in this paragraph were wrong and are corrected in `CHANGELOG.md`/`docs/OUTSTANDING.md`:
> the vocabulary is **19,311** distinct values, not 3,073 (3,073 is the downstream
> winner-cohort evidence's distinct-note count, a subset — sized against it, a follow-on
> plan mis-prices the residual ~5×); and the blast radius that arc actually found was **6
> production call sites in 5 files**, not the 51 `to_confidence` callers named here (that
> count is callers of a signature this arc never had to change). The arc did hash-cons the
> substrate (Step 1 of two); Step 2 — interning the elements — remains parked in
> `docs/OUTSTANDING.md` with its own precondition.
