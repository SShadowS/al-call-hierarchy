// PageExtension generalization of case (b) CONTROL: `BasePage` declares a
// default-visibility `procedure Pub()`. Mirrors the Rust unit test
// `bare_pageextension_base_public_method_control_resolves_to_source`.
page 52910 "BasePage"
{
    procedure Pub()
    begin
    end;
}
