// Case (d) CONTROL: `Base` declares a `protected procedure P()`. Mirrors the
// Rust unit test `bare_extension_base_protected_method_control_resolves_to_source`.
table 52906 "Base"
{
    protected procedure P()
    begin
    end;
}
